import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aiwonsi.lannacalendar',
  appName: 'Lanna Calendar',
  webDir: 'dist'
};

export default config;
